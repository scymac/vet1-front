/*
  ? TESTING

  ? ORDERS

  ?SETUPS
  * prevent deleting setups if they are in use

  ? REPORT
  * plan the report page navigation (show current by default, show another as an option)
  * build the report
  * print as pdf

  ? ERRORS
  * treat list loading errors
  * display error history as a table
  * current errors
  * bind alarm data to tables

  ? PLC
  * make PLC react to start and close of an order

*/
